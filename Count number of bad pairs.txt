class Solution {
public:
    long long countBadPairs(vector<int>& nums) {
        unordered_map<int,int>m;
        long long count  = 0;

        for(int i = 0; i < nums.size(); i++){
            count += (i - m[i - nums[i]]++);
        }
        return count;
    }
};